version: '3.8'

services:
  ijrp_service:
    build:
      context: .
      dockerfile: ./Dockerfile
    ports:
      - '3012:3000'
    volumes:
      - ./:/usr/src/app
    env_file:
      - .env
    command: yarn start:dev
    environment:
      PORT: 3000
    restart: always

